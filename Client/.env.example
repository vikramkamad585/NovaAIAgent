# Client environment variables (Vite inlines VITE_* at build time).
# Dev falls back to localhost automatically if these are unset.
VITE_SERVER_URL=http://localhost:8000
VITE_CLIENT_URL=http://localhost:5173
